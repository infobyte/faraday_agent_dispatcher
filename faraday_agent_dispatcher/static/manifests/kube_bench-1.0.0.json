{
    "arguments": {
        "KUBEBENCH_BENCHMARK": {
            "mandatory": false,
            "type": "string",
            "base": "string"
        },
        "KUBEBENCH_TARGETS": {
            "mandatory": false,
            "type": "string",
            "base": "string"
        },
        "KUBEBENCH_CONFIG_DIR": {
            "mandatory": false,
            "type": "string",
            "base": "string"
        }
    },
    "environment_variables": [],
    "check_cmds": [
        "kube-bench version"
    ],
    "cmd": "{EXECUTOR_FILE_PATH}",
    "category": "Network and Vulnerability Scanners",
    "name": "kube_bench",
    "title": "kube-bench",
    "website": "https://github.com/aquasecurity/kube-bench",
    "description": "CIS Kubernetes Benchmark scanner \u2014 runs the master / etcd / controlplane / node / policies checks against the host where the executor runs. Pick the benchmark version with KUBEBENCH_BENCHMARK (cis-1.8, eks-1.2.0, gke-1.6.0, \u2026) and limit which check groups to run with KUBEBENCH_TARGETS (CSV).",
    "image": null,
    "manifest_version": "1.0.0",
    "repo_executor": "kube_bench.py"
}
