{
    "arguments": {
        "TRUSTAR_ENCLAVE_IDS": {
            "mandatory": false,
            "type": "string",
            "base": "string",
            "description": "Optional comma-separated list of TruSTAR / Splunk Intelligence Management enclave UUIDs to scope the search to. When blank / missing (the typical operational mode for first-time imports), the executor calls /api/1.3/enclaves first to discover every enclave the API key has access to and walks them all. When set, the listed enclave ids are forwarded server-side via the indicators-search body's 'enclaveIds' filter so the dispatcher only pulls IOCs the operator's curated enclaves carry. Whitespace is trimmed and dedupe is case-insensitive while preserving operator order."
        },
        "TRUSTAR_PRIORITY": {
            "mandatory": false,
            "type": "string",
            "base": "string",
            "description": "Optional priority floor \u2014 one of LOW / MEDIUM / HIGH (case-insensitive; aliases critical / crit -> HIGH, moderate / med -> MEDIUM also accepted). Forwarded server-side via the indicators-search body's 'priorityScores' filter as the at-or-above slice (so MEDIUM sends ['MEDIUM', 'HIGH']). Blank / missing keeps every indicator regardless of priority (the typical operational mode); unknown values are rejected client-side with a hard error so a typo doesn't silently widen the result set."
        },
        "TRUSTAR_API_KEY": {
            "mandatory": true,
            "type": "password",
            "base": "string"
        },
        "TRUSTAR_API_SECRET": {
            "mandatory": true,
            "type": "password",
            "base": "string"
        }
    },
    "environment_variables": [
        "TRUSTAR_API_KEY",
        "TRUSTAR_API_SECRET"
    ],
    "check_cmds": [
        "python3 -c 'import requests'"
    ],
    "cmd": "{EXECUTOR_FILE_PATH}",
    "category": "Network and Vulnerability Scanners",
    "name": "trustar",
    "title": "TruSTAR / Splunk Intelligence Management",
    "website": "https://www.splunk.com/en_us/products/intelligence-management.html",
    "description": "TruSTAR / Splunk Intelligence Management importer. Pulls indicator records from the TruSTAR Station REST API (rebranded as Splunk Intelligence Management after the 2021 Splunk acquisition and folded into Splunk SOAR) and emits Faraday bulk-create JSON. TruSTAR is an intelligence-correlation platform that ingests vendor + analyst-curated feeds into operator-scoped enclaves and re-emits a normalised indicator record per IOC tagged with priorityLevel (LOW / MEDIUM / HIGH / NOT_FOUND), correlationCount (how many enclave reports referenced the IOC), and analyst-attached attributes / tags. Endpoints used: GET {TRUSTAR_HOST}/api/1.3/enclaves to discover the enclaves the API key has access to (used as a discovery step when TRUSTAR_ENCLAVE_IDS is blank so first-time imports walk every readable enclave without forcing the operator to enumerate ids by hand; canonical bare-list envelope with items / data / enclaves fallbacks accepted); POST {TRUSTAR_HOST}/api/1.3/indicators/search?pageNumber=N&pageSize=N with the canonical filter body {enclaveIds, priorityScores} (both keys omitted when the corresponding filter is empty, the Station documented behaviour for 'no filter'). Auth: OAuth2 client_credentials \u2014 TRUSTAR_API_KEY + TRUSTAR_API_SECRET are exchanged at POST {TRUSTAR_HOST}/oauth/token with HTTP Basic + form-encoded grant_type=client_credentials body (RFC 6749) for a short-lived (~1h) Bearer token sent as 'Authorization: Bearer ...' on every subsequent v1.3 request. TRUSTAR_HOST defaults to https://api.trustar.co (the canonical Splunk-hosted Station REST host); EU / on-prem / Splunk-SOAR-bundled tenants are tolerated and https:// is added when the operator pasted in a bare FQDN. TRUSTAR_ENCLAVE_IDS is an optional CSV of enclave UUIDs (when blank the executor walks /api/1.3/enclaves first). TRUSTAR_PRIORITY (LOW / MEDIUM / HIGH) is forwarded as the at-or-above slice via priorityScores. Pagination walks pageNumber / pageSize until hasNext flips to False, totalElements is hit, MAX_RESULTS (5000) is reached, or MAX_PAGES (100) is exhausted; inter-request sleep paces requests at 0.4s. Each indicator becomes one Faraday vulnerability under a single synthetic 0.0.0.0 host with hostname 'trustar' (TruSTAR indicators are IOC-keyed not host-keyed \u2014 the operator's other agents emit the host-side findings this feed is correlated against), tagged exactly ['trustar'] per the playbook spec, with a [TruSTAR] name prefix, the canonical indicator type + value + priority + correlation count in the name, and the Station record id in external_id. Severity bucketing: HIGH -> high / MEDIUM -> medium / LOW -> low / NOT_FOUND / missing -> info; HIGH records with correlationCount >= 10 are bumped to critical (heavy cross-enclave correlation is analyst-strong evidence of live activity in the operator's threat landscape). CVE-typed indicators are passed through to the cve list verbatim. Status is always 'open' (a TruSTAR indicator cannot be 'fixed' in the catalog \u2014 it can only be blocked on the operator's perimeter / EDR). Resolution defaults to a type-appropriate blocking recommendation (hashes -> EDR / AV / endpoint prevention; IPs -> firewall + egress proxy + EDR containment; URLs -> egress proxy + endpoint web-filter; domains -> DNS sinkhole + perimeter blocklist; emails -> mail-server blocklist + DMARC quarantine; CVEs -> NVD lookup + patch impacted assets; other types -> generic SOC triage). Refs include the canonical Station web-UI permalink (/constellation/indicator?id=<id>), the canonical NVD CVE permalink for any CVE ids surfaced in the indicator value (when type == CVE) or in free-text notes / tags / attributes (each with an explicit Trustar-CVE pivot), and explicit Trustar-ID / Trustar-Type / Trustar-Value / Trustar-Priority / Trustar-Correlation / Trustar-EnclaveID / Trustar-Tag / Trustar-Attribute / Trustar-FirstSeen / Trustar-LastSeen pivots so operators can pivot from a Faraday finding back to the Station record. Tags: [trustar].",
    "image": null,
    "manifest_version": "1.0.0",
    "repo_executor": "trustar.py"
}
