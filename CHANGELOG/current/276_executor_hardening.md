[FIX] Executor-runtime hardening: `bandit` executor treats empty stdout as a clean scan instead of an error (used to abort with "no data" when a repo had zero Python findings); `theharvester` executor drops its `faraday_plugins` dependency and parses the XML output directly (the upstream plugin lagged behind theHarvester's on-disk schema); `theharvester` is installed from its git upstream instead of PyPI (PyPI wheel was months out of date and missing several source modules); `naabu` gained `TOP_PORTS` validation to catch typos before the scan spawns; `sublist3r` was retired in favor of `subfinder` (upstream sublist3r is unmaintained and its API surface changed on PyPI). #276
