[ADD] `bagre` + `bagre_spray` threat-intel executors vendored into the image. The `offensive_checks/bagre_pkg/` subpackage (clickhouse-driver + intelx sources, credential validators, password-spray engine) is pip-installed into the dispatcher venv at image build so `bagre` / `bagre_spray` can `from bagre.config import load_config` etc. Recategorized manifest as Threat Intelligence. The `bagre.*` imports are deferred to `main()` so the executor module stays importable in environments without the optional deps. Wired into the `threat-intel` / `discovery-osint` agent groups. #276
