[ADD] `agentic-security` agent group with 4 autonomous-AI-pentester connectors: `strix` (Strix.ai Cloud REST client — scans/domains/repositories; per-scan validated vulnerabilities become Faraday vulns bucketed under the target host with CVSS + endpoint + method + PoC + code_diff + remediation folded in), `aikido` (Aikido SAST/DAST/cloud/container `/api/public/v1/issues/export` + attack-surface `/domains` via OAuth2 client-credentials), `xbow` (XBOW autonomous AI-pentester Bearer API), `aws_security_agent` (boto3 `securityagent` client, api version 2025-09-06 — walks `ListAgentSpaces → ListPentests → ListPentestJobsForPentest → BatchGetPentestJobs + ListFindings + BatchGetFindings + ListDiscoveredEndpoints`, requires `boto3>=1.43`). #276
