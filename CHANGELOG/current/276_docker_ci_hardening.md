[FIX] Docker image build + GitLab CI hardening. Publish image now enables `DOCKER_BUILDKIT=1` and pins the frontend to `dockerfile:1.6` for heredoc support. Plugins base image pinned to `kali-last-release` (dodging a systemd 260 postinst regression on kali-rolling that broke sandboxed runners), with `dpkg-divert` stubbing `systemd-machine-id-setup` to `/bin/true` so the postinst survives without a real init system. `masscan` moved into the system-packages layer alongside `libcap2-bin` so the `setcap` fixup runs correctly. CI-side: freed apt cache in the masscan layer, hardened the bandit pre-job against python-version drift, fixed the bandit `after_script` on Debian trixie, marked `docker-integration-build-branch` as `allow_failure: true`, and reduced runner disk pressure by pruning the wheel cache between stages. #276
