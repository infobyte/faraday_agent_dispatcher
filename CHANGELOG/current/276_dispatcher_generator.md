[MOD] `docker/publish/templates/offensive-checks/generate_dispatcher_manifest.py` hardening: added `--image-pull-secret` for pulling from private registries, set `imagePullPolicy: Always` on the dispatcher pod so re-deploys pick up new image tags, expanded per-agent-group descriptions with per-executor auth flow + endpoint + scope notes (WIZ_CLIENT_ID → OAuth, RHACS Bearer, Prisma `x-redlock-auth`, JWT HS256, etc.), and expanded per-executor manifest descriptions to name modes, pagination, and severity mapping. #276
