[ADD] Bundle offensive-check manifests in the dispatcher package + surface them to `faraday_agent_parameters_types` at build time. `MANIFEST.in`, `pyproject.toml`, and `setup.cfg` updated so the vendored manifests under `offensive_checks/manifests/` are shipped inside the wheel and mirrored into `faraday_agent_parameters_types/static/manifests/` during Dockerfile stages. Fixes `python -c` quoting in the parameters_types manifest copy step. #276
