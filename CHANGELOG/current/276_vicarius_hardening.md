[MOD] Vicarius vRx executor hardening: added `software` / `incidents` modes on top of the existing `assets` / `cves` / `patches`, per-mode error isolation so one broken endpoint doesn't abort the whole run, auto-pagination with 429 back-off, dedup by `external_id` before `bulk_create`, and fixed the assets mode which was incorrectly using `endpointName` as the host IP. Also fixed CVE + patch field extraction paths (`/organizationEndpointVulnerabilities/search` + `/organizationEndpointExternalReferenceExternalReferences/search`). #276
