[FIX] Roll back `mandatory: true` to `mandatory: false` on credential-shaped manifest arguments (`type: password` + `*_USERNAME`/`*_USER`/`*_CLIENT_ID`/`*_TENANT_ID`) across 147 executors. The bulk env-var-to-per-scan-arg promotion accidentally made every credential a required per-scan input even when the agent already had a varenv fallback, blocking every SAST/secrets/IaC scan with `Mandatory argument GIT_USERNAME not passed`. Restoring `mandatory: false` lets Faraday fall back to the agent's varenv when no per-scan value is supplied (operators can still override per-scan from the UI). #276
