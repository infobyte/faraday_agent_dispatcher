[FIX] `sonarqube` executor: stop leaking the auth token in log output (previously echoed the `-u <token>:` curl fragment on the "no response" error path), stop infinite-looping on empty JSON responses (returned an unhandled `ValueError` that retried indefinitely), and read `EXECUTOR_CONFIG_SONAR_URL` before falling back to `SONAR_URL` env for consistency with the other executors. #276
