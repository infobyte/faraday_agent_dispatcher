[ADD] `governance-compliance` agent group with 3 GRC platform connectors: `vanta` (OAuth2 client-credentials against `api.vanta.com`, imports control tests and framework-mapped findings), `drata` (Bearer API key against `public-api.drata.com`, imports tests + controls), `sprinto` (Bearer API key against `api.sprinto.com`, imports checks + controls). Each surfaces failing GRC control tests as Faraday vulnerabilities on the affected compliance-scope host, so operators can triage SOC 2 / ISO 27001 / HIPAA / GDPR gaps alongside scanner findings. #276
