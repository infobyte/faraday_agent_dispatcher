[MOD][BREAKING] Rename the deployment template directory `docker/publish/templates/vicarius/` to `docker/publish/templates/offensive-checks/` and genericize its `README.md` + `generate_dispatcher_manifest.py` so the runbook, k8s labels (`app.kubernetes.io/instance: offensive-checks`), argparse defaults (`--namespace offensive-checks`, `--deployment offensive-checks-agent-dispatcher`, `--agent-name offensive-checks-dispatcher`, group deployment suffix `offensive-checks-<group>-dispatcher`), and description strings are all tenant-neutral. `--host` is now required (previously defaulted to a specific tenant hostname). Anyone driving the old path with automation must update the invocation. #276
