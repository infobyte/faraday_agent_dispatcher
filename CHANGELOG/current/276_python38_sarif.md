[FIX] Python 3.8 compat across all offensive-check executors — added `from __future__ import annotations` so PEP-604 union syntax (`int | None`) doesn't break unit tests running on 3.8/3.9/3.10. Also patched the bundled `faraday_plugins` SARIF parser to handle tools that omit `fullDescription` from rule metadata (previously crashed on the semgrep + gitleaks + trufflehog outputs). #276
