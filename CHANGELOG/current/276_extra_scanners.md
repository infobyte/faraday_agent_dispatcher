[ADD] Additional standalone scanner executors — `edgescan` (Edgescan vulnerability-management SaaS, Bearer API-token auth), `vulndb` (Risk Based Security VulnDB CVE enrichment, HTTP Basic against `vulndb.cyberriskanalytics.com`), and `tripwire_ip360` (VnE vulnerability-management on-prem, SOAP-ish `/reports/api` walk). Each ships with the paired `.py` + `.json` and follows the fleet's standard EXECUTOR_CONFIG_* auth + paginated walk + severity normalization + stable external_id conventions. #276
