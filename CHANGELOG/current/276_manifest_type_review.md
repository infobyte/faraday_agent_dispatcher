[FIX] Manifest type-annotation review across every executor arg. Reverted 18 enum-shaped fields from `type: list` back to `type: string` (Faraday's UI renders `list` as a multi-select which broke free-form comma-separated inputs). Corrected `type` for URL-shaped fields (`url`), integer counters (`integer`), and true multi-value fields (`list`). Enriched arg descriptions to enumerate valid enum values inline so operators don't need to grep the executor source. #276
