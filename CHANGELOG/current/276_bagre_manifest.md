[FIX] `bagre` + `bagre_spray` manifest hygiene: every argument now carries a `description` (previously all 30+ fields shipped with empty strings so the WebUI Run form had no hints), the 7 password-spray safety knobs (`BAGRE_PASSWORD_SPRAY_MAX_ATTEMPTS_PER_USER` / `_DELAY_MS` / `_TIMEOUT_S` / `_MAX_TOTAL_ATTEMPTS` / `_CONCURRENCY` / `_EXCLUDE_USERS` / `_DRY_RUN`) are re-typed from `password` (which masked them as sensitive inputs) to `integer` / `string` so operators can see and edit them, `CLICKHOUSE_HOST` re-typed from `url` to `string` (ClickHouse hostnames are bare `host:port`, not URLs), and the several IntelX/ClickHouse endpoint fields that were mandatory but only apply to one of the two possible `BAGRE_SOURCE` modes were flipped to `mandatory: false` so an operator running against IntelX doesn't have to fill ClickHouse coords (and vice versa). #276
